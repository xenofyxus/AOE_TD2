import React from 'react'
import { render } from 'react-dom'
import {unitSprite} from '../assets/AOE_TD_LOGO.png';
export function Player(props){

        return(
            props.mapComponents
        )
}

function mapStateToProps(state) {
    return { 

    }

}

